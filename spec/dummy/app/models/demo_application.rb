class DemoApplication < ApplicationRecord
  # Placeholder model for managing OAuth applications in the playground
  # Fields (future): name, client_id, client_secret, redirect_uris:text, scopes, active:boolean
end
